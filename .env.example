# Required environment variables

# LlamaCloud (for RAG indices)
LLAMA_CLOUD_API_KEY=your_llama_cloud_api_key
LLAMA_CLOUD_ORG_ID=your_llama_cloud_org_id

# OpenAI (for main agent)
OPENAI_API_KEY=your_openai_api_key
# AGENT_MODEL=gpt-5.1  # Options: gpt-5.2, gpt-5.1, gpt-4o

# Slack (for bot integration)
SLACK_BOT_TOKEN=xoxb-your-slack-bot-token

# Linear API key
LINEAR_API_KEY=your_linear_api_key

# Server settings
BASE_URL=http://localhost:8000
PORT=8000
IMAGE_DIR=/tmp/images

# Optional: Wizard API (for Allora query tools)
WIZARD_API_URL=http://localhost:3000
# WIZARD_API_KEY=your_api_key  # Only needed if wizard auth is enabled

# MCP Wizard Server (for direct wizard tool integration)
# Two modes:
# - Production (Docker): use binary name directly (installed globally during build)
#   MCP_WIZARD_PACKAGE=allora-wizard-mcp
# - Development: use scoped package name (requires npm auth for GitHub Packages)
#   MCP_WIZARD_PACKAGE=@allora-network/wizard-mcp
MCP_WIZARD_PACKAGE=allora-wizard-mcp
